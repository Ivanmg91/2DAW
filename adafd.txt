usando flexbox tansolo podemos colocar elementos en una sola direcciona a la vez mientras q usando grid podemos colocar filas y columnas simultaneamente creando una estructura cuadricular. Usando flexbox el tamaño de las celdas se ajusta segun su contenido mientras q con grdi defines la estructura y el contenido no la modifica. SIMILITUD: en ambos podemos usar las propiedades align y demás para posicionar el contenido

2. display: grid;
grid-template-columns: 300px 2fr 1fr;
grid-template-rows: 12em auto 100px

3. test

4. #gallery {
  column-gap: 1em;
}

 freestyle
